#include <stdio.h>
	int main(){
		float area, length, width;
			printf("Enter length here: \n");
				scanf("%f", &length);
			printf("Enter width here: \n");
				scanf("%f", &width);
		area = length * width;
			printf("%.2f", area);
	return 0;	 	 
}

